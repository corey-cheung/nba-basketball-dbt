
version: 2

models:
  - name: team
    description: "Mart table for NBA games"
    columns:
      - name: team_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
